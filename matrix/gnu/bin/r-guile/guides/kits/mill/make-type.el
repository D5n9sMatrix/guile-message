;; make-type.el

;; extension name: make-type
;; obvjective ext: polony
;; form ext: guile
;; gnu hack: GNU Guile make-type Manual
;; url: http://gnu.org

;; Terminal console modules
(point-marker)
;; Input Terminal
:R1
;; select type style REPL message
(selected-window)
;; Input terminal
:R3
;; values frames selected
(selected-frame)
;; Input Terminal
:R1
;; Terminal new
(get-device-terminal nil)
;; Input Terminal
:R3
;; process of list in arguments
(process-list)
:R1
;; all threads logic of moffew
(all-threads)
:R3
;; fonts to the object
(prog1 nil
  (setq x '(#1=(a) b #1#)))
(eq (nth 0 x) (nth 2 x))
(setq x '((a) b (a)))
(eq (nth 0 x) (nth 2 x))
;; running path slinte
(prog1 nil
  (setq x '#1=(a #1#)))
(eq x (cadr x))

(defun add-on (x)
  (cond ((symbolp x)))
  ;; If tamping is my friend it is good ?
  (setq list (cons x list)))
;; its object there good selected ?
(type-of 512)
;; pipeline private output object
(type-of 'nil)
;; coffee content or context
(type-of '(x))
;; happen brother study words
(type-of (record 'foo))
;; if your is one word what me say ?
(eq 'foo 'foo)
;; what good ice9 Continue climatic -27 -0 degreen
(eq ?A ?A)
;; what good is this cold ice9 climatic Continue -27 -0 degreen
;; pass to celsious level Cº template ambient
(eq 3.0 3.0)
