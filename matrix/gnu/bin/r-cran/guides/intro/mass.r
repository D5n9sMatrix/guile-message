#!/usr/bin/r

# form utils to light it recheable
alert_success <- table(c(1, 2, 3))
# delta response ability
alert_info <- table(c(1:9))
# template kits
alert_danger <- table(c(1:10))

# message wine values
x <- 1
# index values packages
index <- 512
# function checkup
fun <- NULL
# tools danger
tapply(x, index, fun = NULL, simplify = TRUE)
